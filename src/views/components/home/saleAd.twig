<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .tg-tickerbox {
            background-color: black;
            width: 100%;
            float: left;
            padding: 0;
            position: relative;
            border-bottom: 1px solid #ddd;
            overflow: hidden;
        }

        .ticker-content {
            padding: 10px;
        }

        .marquee-container {
            white-space: nowrap;
            overflow: hidden;
            animation: marqueeAnimation 15s linear infinite; /* Adjust the duration as needed */
        }

        .marquee-content {
            display: inline-block;
            padding-top: 5px;
            margin-right: 30px;
            color: #fff;
            text-decoration: none;
        }

        .tg-tickerbox:hover .marquee-container {
            animation-play-state: paused;
        }

        @keyframes marqueeAnimation {
            0% { transform: translateX(100%); }
            100% { transform: translateX(-100%); }
        }
    </style>
</head>
<body>

<div class="tg-tickerbox test">
    <div class="ticker-content">
        <div class="marquee-container">
            {% for offer in component.addoffer %}
                <a href="https://salla.design/dev-adn671ta9breebis/offers" class="marquee-content" target="_blank">{{offer.offer}}</a>
            {% endfor %}
        </div>
    </div>
</div>

</body>
</html>
